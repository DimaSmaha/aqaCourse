/** TASK 15 - Generating Fake Data with Faker.js
 *
 * 1. Install faker.js using npm
 *
 * 2. Import the faker library.
 *
 * 3. Create a fake user object with properties:
 *    - firstName
 *    - lastName
 *    - phoneNumber
 *
 * 4. Output the generated user object to the console.
 *
 * 5. Create a variable and assign an arrow function that generates faker address
 *
 * 6. Create a variable that just generates a faker address
 *
 * 7. Output both into console, 2 times each. Why in the case with arrow function we have different value?
 *
 */
