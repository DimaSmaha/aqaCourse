/** TASK - Using the Map Method
 *
 * 1. Declare an array "numbers" with at least five different numbers.
 *
 * 2. Use the map method to create a new array "squaredNumbers":
 *    - Each element should be the square of the corresponding element in "numbers".
 *
 * 3. Output both arrays to the console.
 */

const numbers = [2, 4, 6, 8, 10];

// create a new array using .map method

console.log("Original numbers:", numbers);
console.log("Squared numbers:", squaredNumbers);
